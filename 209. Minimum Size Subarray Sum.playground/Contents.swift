import UIKit

class Solution {
    func minSubArrayLen(_ target: Int, _ nums: [Int]) -> Int {
        
        return 0
    }
}

var sol = Solution()
var test = [0]
var target = 0
var answer = sol.minSubArrayLen(target, test)
print(answer)
